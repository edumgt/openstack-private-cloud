---
- name: "Nova 인스턴스 라이프사이클 자동화"
  hosts: all
  become: true
  gather_facts: true
  vars:
    lecture_id: "lecture16"
    lecture_title: "Nova 인스턴스 라이프사이클 자동화"
    install_enabled: false
    reference_playbook: "../ansible/playbooks/12_deploy_stack_rolling.yml"
    lecture_packages:
      - python3
      - python3-pip
      - git
      - curl
      - jq
  tasks:
    - name: Show lecture context
      ansible.builtin.debug:
        msg:
          - "id={{ lecture_id }}"
          - "title={{ lecture_title }}"
          - "reference_playbook={{ reference_playbook }}"
          - "install_enabled={{ install_enabled }}"

    - name: Install lecture packages when enabled
      ansible.builtin.package:
        name: "{{ lecture_packages }}"
        state: present
      when: install_enabled | bool

    - name: Check Python3 availability
      ansible.builtin.command: python3 --version
      register: python_version
      changed_when: false
      failed_when: false

    - name: Print Python3 version
      ansible.builtin.debug:
        var: python_version.stdout
