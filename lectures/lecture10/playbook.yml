---
- name: "Compose 배포와 업데이트 전략"
  hosts: all
  become: true
  gather_facts: true
  vars:
    lecture_id: "lecture10"
    lecture_title: "Compose 배포와 업데이트 전략"
    install_enabled: false
    reference_playbook: "../ansible/playbooks/11_deploy_stack.yml"
    lecture_packages:
      - python3
      - python3-pip
      - git
      - curl
      - jq
      - docker.io
  tasks:
    - name: Show lecture context
      ansible.builtin.debug:
        msg:
          - "id={{ lecture_id }}"
          - "title={{ lecture_title }}"
          - "reference_playbook={{ reference_playbook }}"
          - "install_enabled={{ install_enabled }}"

    - name: Install lecture packages when enabled
      ansible.builtin.package:
        name: "{{ lecture_packages }}"
        state: present
      when: install_enabled | bool

    - name: Check Python3 availability
      ansible.builtin.command: python3 --version
      register: python_version
      changed_when: false
      failed_when: false

    - name: Print Python3 version
      ansible.builtin.debug:
        var: python_version.stdout
